"""URL routes for the queue_management app. Views will be added in the Backend Development phase."""
from django.urls import path

app_name = "queue_management"

urlpatterns = [
    # Populated in later phases.
]
